# Default — SQLite local, zéro install externe.
DB_DIALECT=sqlite
SGBD_URI=./app.db

# Ce sample ne lit pas .env (config est en dur dans app.ts).
# Voir sample 02-multi-dialect-switch pour l'usage de getConfigFromEnv().
